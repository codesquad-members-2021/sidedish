import styled from "styled-components";

const FlexRowContainer = styled.div`
    display:flex;
    flex-direction: row;
`
const FlexColContainer = styled.div`
    display:flex;
    flex-direction: col;
`

export  { FlexRowContainer, FlexColContainer }