import HeaderLeft from './HeaderLeft';
import HeaderRight from './HeaderRight';

const Header = (props) => {
  return (
    <header>
      <HeaderLeft />
      <HeaderRight />
    </header>
  );
};

export default Header;
