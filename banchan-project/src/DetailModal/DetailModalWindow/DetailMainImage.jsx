import { Card } from "../../Styles/commonStyles";

const DetailMainImage = ({ topImage }) => {
  return <Card.CardImage width={392} height={392} imgUrl={topImage} />;
};

export default DetailMainImage;
